<h6>Submit a Comment</h6>
<%= form_for(:comment) do |f| %>
	<% if question_id %>
		<%= f.hidden_field :commentable, :value => question_id %>
	<% elsif answer_id %>
		<%= f.hidden_field :commentable, :value => answer_id %>
	<% end %>
	<%= f.text_area :text, size: "50x4" %>
<% end %>
  